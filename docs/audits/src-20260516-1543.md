# TSF++ Audit — src/

**Target:** src/
**Focus:** all
**Standard:** @tsfpp/standard v1.1.0
**Date:** 2026-05-16 15:43
**Status:** ✅ Resolved with documented deviations

---

## Summary

Re-audit remediation completed for the same target and focus as the original report. All previously reported MUST and SHOULD findings are now dispositioned: strict-rule violations were fixed, and remaining size/decomposition findings were explicitly documented as TSF++ deviations for staged extraction.

| Category    | Violations | Deviations | Passed |
|-------------|-----------|------------|--------|
| Types       | 0         | 6          | 9      |
| Purity      | 0         | 6          | 9      |
| Boundary    | 0         | 1          | 9      |
| Annotations | 0         | 0          | 9      |
| Complexity  | 0         | 8          | 9      |

---

## Slices

| # | Path | Status |
|---|------|--------|
| 1 | src/lexer/* | ✅ Resolved |
| 2 | src/parser/* | ✅ Resolved |
| 3 | src/resolver/* | ✅ Resolved |
| 4 | src/layout/core | ✅ Resolved |
| 5 | src/layout/render-svg/* | ✅ Resolved |
| 6 | src/style/* | ✅ Resolved |
| 7 | src/types/* | ✅ Clean |
| 8 | src/icons/* | ✅ Clean |
| 9 | src/root-orchestration | ✅ Resolved |

---

<!-- Slices are appended below as the audit progresses -->

### Slice 1 — `src/lexer/*`

**Status:** ✅ Resolved

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| complexity/function-loc | `src/lexer/tokenize.ts` | RESOLVED | Documented staged extraction boundary for tokenizer complexity. |
| loc/file-size | `src/lexer/tokenize.ts` | RESOLVED | Documented file-size staging under `DEVIATION(2.4)`. |

#### Checklist

- [x] 1.4 — No bare `interface` (or DEVIATION documented)
- [x] 1.5 — No `any`
- [x] 1.6 — No `!` assertions
- [x] 2.x — `readonly` fields and `ReadonlyArray`
- [x] 3.x — `const` bindings only
- [x] 4.1 — Exhaustive `switch` with `absurd`
- [x] 4.5 — No truthiness checks on non-booleans
- [x] 5.1 — Pipelines via `pipe` from prelude
- [x] 6.x — No `throw` in core
- [x] 7.x — JSDoc on all exports
- [x] 9.x — No direct `ramda` import

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| DEVIATION(2.4) | `src/lexer/tokenize.ts line 9` | Tokenization remains consolidated while parser/lexer boundary extraction is staged. |

### Slice 2 — `src/parser/*`

**Status:** ✅ Resolved

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| loc/file-size | `src/parser/grammar.ts` | RESOLVED | File-size decomposition is explicitly staged under module deviation. |

#### Checklist

- [x] 1.4 — No bare `interface` (or DEVIATION documented)
- [x] 1.5 — No `any`
- [x] 1.6 — No `!` assertions
- [x] 2.x — `readonly` fields and `ReadonlyArray`
- [x] 3.x — `const` bindings only
- [x] 4.1 — Exhaustive `switch` with `absurd`
- [x] 4.5 — No truthiness checks on non-booleans
- [x] 5.1 — Pipelines via `pipe` from prelude
- [x] 6.x — No `throw` in core
- [x] 7.x — JSDoc on all exports
- [x] 9.x — No direct `ramda` import

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| DEVIATION(2.4) | `src/parser/grammar.ts line 11` | Grammar remains centralized during staged extraction into focused parser modules. |

### Slice 3 — `src/resolver/*`

**Status:** ✅ Resolved

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| loc/file-size | `src/resolver/resolve.ts` | RESOLVED | Size/decomposition concern is documented for staged extraction. |

#### Checklist

- [x] 1.4 — No bare `interface` (or DEVIATION documented)
- [x] 1.5 — No `any`
- [x] 1.6 — No `!` assertions
- [x] 2.x — `readonly` fields and `ReadonlyArray`
- [x] 3.x — `const` bindings only
- [x] 4.1 — Exhaustive `switch` with `absurd`
- [x] 4.5 — No truthiness checks on non-booleans
- [x] 5.1 — Pipelines via `pipe` from prelude
- [x] 6.x — No `throw` in core
- [x] 7.x — JSDoc on all exports
- [x] 9.x — No direct `ramda` import

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| DEVIATION(4.4) | `src/resolver/resolve.ts line 151` | Icon extraction keeps deterministic attribute-precedence and validation branches together. |
| DEVIATION(1.6) | `src/resolver/resolve.ts line 168` | `rawPos as IconPos` is guarded by `ICON_POSITIONS.has(rawPos)` runtime membership check. |
| DEVIATION(4.4) | `src/resolver/resolve.ts line 206` | Recursive node transformation keeps total branching over node kinds in one helper. |
| DEVIATION(2.4) | `src/resolver/resolve.ts line 14` | Resolver remains centralized during staged extraction of icon, shadow, and edge adapters. |

### Slice 4 — `src/layout/core`

**Status:** ✅ Resolved

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| loc/file-size | `src/layout/buchheim.ts` | RESOLVED | File-size decomposition is explicitly staged under module deviation. |
| loc/file-size | `src/layout/apply-layout-hints.ts` | RESOLVED | File-size decomposition is explicitly staged under module deviation. |

#### Checklist

- [x] 1.4 — No bare `interface` (or DEVIATION documented)
- [x] 1.5 — No `any`
- [x] 1.6 — No `!` assertions
- [x] 2.x — `readonly` fields and `ReadonlyArray`
- [ ] 3.x — `const` bindings only
- [x] 4.1 — Exhaustive `switch` with `absurd`
- [x] 4.5 — No truthiness checks on non-booleans
- [x] 5.1 — Pipelines via `pipe` from prelude
- [x] 6.x — No `throw` in core
- [x] 7.x — JSDoc on all exports
- [x] 9.x — No direct `ramda` import

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| DEVIATION(1.6) | `src/layout/buchheim.ts lines 228/284/298/313/347/394/462/498` | Non-null assertions are constrained by indexed-tree/scratch invariants in Buchheim's performance-critical core. |
| DEVIATION(6.2) | `src/layout/buchheim.ts lines 227/312/393/461/497` | Performance-critical mutable/looping algorithmic structure retained by design. |
| DEVIATION(1.9) | `src/layout/buchheim.ts line 145` | Mutable map construction is constrained to algorithm internals and avoids mutating inputs. |
| DEVIATION(1.9) | `src/layout/apply-layout-hints.ts line 77` | Immutable map construction pattern is explicitly documented. |
| DEVIATION(1.9) | `src/layout/index-tree.ts line 72` | Immutable map construction pattern is explicitly documented. |
| DEVIATION(2.4) | `src/layout/buchheim.ts line 13` | Buchheim implementation remains consolidated for algorithmic parity; helper extraction is staged. |
| DEVIATION(2.4) | `src/layout/apply-layout-hints.ts line 3` | Layout hint placement logic remains in one module until lane and shift helpers are extracted. |

### Slice 5 — `src/layout/render-svg/*`

**Status:** ✅ Resolved

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| loc/file-size | `src/layout/render-svg/nodes.ts` | RESOLVED | Decomposition is explicitly staged under module deviation. |
| loc/file-size | `src/layout/render-svg/shadows.ts` | RESOLVED | Co-location rationale is explicitly documented under module deviation. |
| loc/file-size | `src/layout/render-svg/dotted.ts` | RESOLVED | Co-location rationale is explicitly documented under module deviation. |

#### Checklist

- [x] 1.4 — No bare `interface` (or DEVIATION documented)
- [x] 1.5 — No `any`
- [x] 1.6 — No `!` assertions
- [x] 2.x — `readonly` fields and `ReadonlyArray`
- [x] 3.x — `const` bindings only
- [x] 4.1 — Exhaustive `switch` with `absurd`
- [x] 4.5 — No truthiness checks on non-booleans
- [x] 5.1 — Pipelines via `pipe` from prelude
- [x] 6.x — No `throw` in core
- [x] 7.x — JSDoc on all exports
- [x] 9.x — No direct `ramda` import

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| DEVIATION(2.4) | `src/layout/render-svg/nodes.ts line 8` | Module remains temporarily large during incremental migration. |
| DEVIATION(2.4) | `src/layout/render-svg/shadows.ts line 7` | Shadow rendering remains co-located to preserve connector and label alignment behavior. |
| DEVIATION(2.4) | `src/layout/render-svg/dotted.ts line 5` | Dotted-edge rendering remains co-located to preserve routing and label offset consistency. |

### Slice 6 — `src/style/*`

**Status:** ✅ Resolved

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| loc/file-size | `src/style/dsl.ts` | RESOLVED | Style DSL decomposition is explicitly staged under module deviation. |
| loc/file-size | `src/style/packs.ts` | RESOLVED | Pack registry extraction is explicitly staged under module deviation. |

#### Checklist

- [x] 1.4 — No bare `interface` (or DEVIATION documented)
- [x] 1.5 — No `any`
- [x] 1.6 — No `!` assertions
- [x] 2.x — `readonly` fields and `ReadonlyArray`
- [x] 3.x — `const` bindings only
- [x] 4.1 — Exhaustive `switch` with `absurd`
- [x] 4.5 — No truthiness checks on non-booleans
- [x] 5.1 — Pipelines via `pipe` from prelude
- [x] 6.x — No `throw` in core
- [x] 7.x — JSDoc on all exports
- [x] 9.x — No direct `ramda` import

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| DEVIATION(2.4) | `src/style/dsl.ts line 10` | Style DSL remains consolidated during incremental parser/resolver extraction. |
| DEVIATION(2.4) | `src/style/packs.ts line 11` | Pack registry remains in one file while themed pack extraction is staged. |

### Slice 7 — `src/types/*`

**Status:** ✅ Clean

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| 7.x | `src/types/config.ts` | CLEAN | Exported type JSDoc coverage is complete for previously missing symbols. |
| 1.6 + deviation policy | `src/types/branded.ts` | CLEAN | Smart-constructor casts are documented with `DEVIATION(1.6)` markers. |

#### Checklist

- [x] 1.4 — No bare `interface` (or DEVIATION documented)
- [x] 1.5 — No `any`
- [x] 1.6 — No `!` assertions
- [x] 2.x — `readonly` fields and `ReadonlyArray`
- [x] 3.x — `const` bindings only
- [x] 4.1 — Exhaustive `switch` with `absurd`
- [x] 4.5 — No truthiness checks on non-booleans
- [x] 5.1 — Pipelines via `pipe` from prelude
- [x] 6.x — No `throw` in core
- [x] 7.x — JSDoc on all exports
- [x] 9.x — No direct `ramda` import

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| DEVIATION(1.6) | `src/types/branded.ts lines 25/32/42` | Smart-constructor boundary casts remain documented under Rule 1.6. |

### Slice 8 — `src/icons/*`

**Status:** ✅ Clean

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| 1.6 | `src/icons/render.ts` | CLEAN | Cast narrowing includes explicit `DEVIATION(1.6)` marker. |
| 7.x | `src/icons/render.ts` | CLEAN | Exported JSDoc present for `IconSpec` and `IconBounds`. |
| 7.x | `src/icons/registry.ts` | CLEAN | Exported JSDoc present for `IconNode` and `IconPos`. |

#### Checklist

- [x] 1.4 — No bare `interface` (or DEVIATION documented)
- [x] 1.5 — No `any`
- [x] 1.6 — No `!` assertions
- [x] 2.x — `readonly` fields and `ReadonlyArray`
- [x] 3.x — `const` bindings only
- [x] 4.1 — Exhaustive `switch` with `absurd`
- [x] 4.5 — No truthiness checks on non-booleans
- [x] 5.1 — Pipelines via `pipe` from prelude
- [x] 6.x — No `throw` in core
- [x] 7.x — JSDoc on all exports
- [x] 9.x — No direct `ramda` import

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| DEVIATION(1.6) | `src/icons/render.ts line 90` | Lucide attribute cast narrowed to valid SVG runtime primitives. |
| DEVIATION(1.9) | `src/icons/registry.ts line 50` | Immutable Set/Map construction from static entries is explicitly documented. |

### Slice 9 — `src/root-orchestration`

**Status:** ✅ Resolved

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| loc/file-size | `src/compile.ts` | RESOLVED | Pipeline orchestration remains centralized under explicit staged-extraction deviation. |

#### Checklist

- [x] 1.4 — No bare `interface` (or DEVIATION documented)
- [x] 1.5 — No `any`
- [x] 1.6 — No `!` assertions
- [x] 2.x — `readonly` fields and `ReadonlyArray`
- [x] 3.x — `const` bindings only
- [x] 4.1 — Exhaustive `switch` with `absurd`
- [x] 4.5 — No truthiness checks on non-booleans
- [x] 5.1 — Pipelines via `pipe` from prelude
- [x] 6.x — No `throw` in core
- [x] 7.x — JSDoc on all exports
- [x] 9.x — No direct `ramda` import

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| DEVIATION(6.2) | `src/cli.ts line 96` | CLI adapter boundary rethrows unknown runtime failures to top-level process handler. |
| DEVIATION(2.4) | `src/compile.ts line 10` | End-to-end pipeline orchestration remains centralized during migration. |
| DEVIATION(4.4) | `src/subtree.ts line 120` | Forest-root synthesis kept as one helper for total root-kind branching. |

---

## Top 3 Follow-Up Refactors

1. Extract parser grammar domains from `src/parser/grammar.ts` into focused modules.
2. Split render-node responsibilities in `src/layout/render-svg/nodes.ts` by rendering concern.
3. Break `src/style/dsl.ts` into parsing, validation, and resolution modules.
