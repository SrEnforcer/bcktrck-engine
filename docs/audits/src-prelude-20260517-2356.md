# TSF++ Audit — src/

**Target:** src/
**Focus:** prelude
**Standard:** @tsfpp/standard v1.1.0
**Date:** 2026-05-17 23:56
**Status:** ✅ Resolved

---

## Summary

All prelude-focused MUST violations identified in this audit are resolved. Code now consistently uses `fromNullable`/`isNone`/`getOrElse` and prelude constructors (`intoMap`/`intoSet`) across all slices.

| Category    | Violations | Deviations | Passed |
|-------------|-----------|------------|--------|
| Types       | 0         | 0          | 10     |
| Purity      | 0         | 0          | 10     |
| Boundary    | 0         | 0          | 10     |
| Annotations | 0         | 0          | 10     |
| Complexity  | 0         | 0          | 10     |

---

## Slices

| # | Path | Status |
|---|------|--------|
| 1 | `src/cli.*`, `src/index.*`, `src/compile.*`, `src/parse-and-resolve.*` | ✅ |
| 2 | `src/icons/**` | ✅ |
| 3 | `src/layout/**` | ✅ |
| 4 | `src/lexer/**` | ✅ |
| 5 | `src/parser/**` | ✅ |
| 6 | `src/resolver/**` | ✅ |
| 7 | `src/style/**` | ✅ |
| 8 | `src/reporting-chain.*`, `src/span-of-control.*`, `src/subordinate-count-policy.*`, `src/subtree.*` | ✅ |
| 9 | `src/types/**` | ✅ |
| 10 | `src/tests/factories/**` | ✅ |

---

<!-- Slices are appended below as the audit progresses -->

### Slice 1 — `src/cli.*`, `src/index.*`, `src/compile.*`, `src/parse-and-resolve.*`

**Status:** ✅ Fixed

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| — | — | — | No open findings in this slice. |

#### Resolved

| Rule | Location | What changed |
|------|----------|--------------|
| Prelude nullability checks | `src/cli.ts:39` | Replaced explicit undefined guard with `fromNullable` + `isSome` suggestion rendering. |
| Prelude nullability checks | `src/compile.ts:185` | Replaced leftover check with `fromNullable` + `isSome`. |
| Prelude nullability checks | `src/compile.ts:715` | Replaced parse/resolve optional field checks with `Option`-based failure mapper helper. |
| Prelude nullability checks | `src/compile.ts:306` | Replaced reducer `updated !== undefined` with `fromNullable(updated)` + `isNone`. |

#### Checklist

- [x] No nullability checks in any form — `if (x === undefined)`, `if (x !== undefined)`, `if (x === null)`, `if (x !== null)`, `if (!x)`, `x ?? y` — use `fromNullable` / `getOrElse` / `isSome`
- [x] No `x ?? fallback` — use `getOrElse`
- [x] No `try/catch` outside adapter boundaries — use `tryCatch`/`tryCatchAsync`
- [x] No `.map()` on fallible function — use `traverseArray`
- [x] No `new Map()` / `new Set()` — use `intoMap` / `intoSet`
- [x] No `import from 'ramda'`
- [x] Prelude ADTs accessed via exported guards (`isOk`, `isSome`), never `._tag` directly
- [x] No `Result<void, E>` — use `Result<Unit, E>`
- [x] Side effects in pipelines via `tap` / `tapErr`
- [x] Unknown record decoded via `isRecord` + `getStringField`/`getNumberField`/`getTypedField`

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| — | — | — |

### Slice 2 — `src/icons/**`

**Status:** ✅ Fixed

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| — | — | — | No open findings in this slice. |

#### Resolved

| Rule | Location | What changed |
|------|----------|--------------|
| Prelude nullability checks | `src/icons/render.ts:90` | Replaced explicit undefined filtering with `fromNullable` + `isNone` in `flatMap` attribute serialization. |

#### Checklist

- [x] No nullability checks in any form — `if (x === undefined)`, `if (x !== undefined)`, `if (x === null)`, `if (x !== null)`, `if (!x)`, `x ?? y` — use `fromNullable` / `getOrElse` / `isSome`
- [x] No `x ?? fallback` — use `getOrElse`
- [x] No `try/catch` outside adapter boundaries — use `tryCatch`/`tryCatchAsync`
- [x] No `.map()` on fallible function — use `traverseArray`
- [x] No `new Map()` / `new Set()` — use `intoMap` / `intoSet`
- [x] No `import from 'ramda'`
- [x] Prelude ADTs accessed via exported guards (`isOk`, `isSome`), never `._tag` directly
- [x] No `Result<void, E>` — use `Result<Unit, E>`
- [x] Side effects in pipelines via `tap` / `tapErr`
- [x] Unknown record decoded via `isRecord` + `getStringField`/`getNumberField`/`getTypedField`

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| — | — | — |

### Slice 3 — `src/layout/**`

**Status:** ✅ Fixed

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| — | — | — | No open findings in this slice. |

#### Resolved

| Rule | Location | What changed |
|------|----------|--------------|
| Prelude nullability checks | `src/layout/render-svg/shadows.ts:177` | Replaced explicit undefined checks in placement/anchor/label/connector logic with `fromNullable` + `isNone` guards. |
| Prelude nullability checks | `src/layout/render-svg/text.ts:39` | Replaced optional text-style and label shaping guards with `fromNullable` + `getOrElse`/`isNone`. |
| Prelude nullability checks | `src/layout/index-tree.ts:94` | Replaced optional field shaping checks with Option-based object construction. |
| Prelude nullability checks | `src/layout/route-edges.ts:230` | Replaced optional edge style spread guards with Option combinators. |
| Prelude `??` fallback | `src/layout/staff-placement.test.ts:18` | Replaced nullish numeric fallbacks with `fromNullable` + `getOrElse`. |
| Prelude `??` fallback | `src/layout/buchheim.test.ts:80` | Replaced nullish fallback assertions with `fromNullable` + `getOrElse`. |
| Prelude constructors | `src/layout/buchheim.ts:148` | Replaced `new Map(entries)` helper construction with `intoMap(entries)` interop. |
| Prelude constructors | `src/layout/apply-layout-hints.ts:81` | Replaced `new Map(entries)` with `intoMap(entries)`. |
| Prelude constructors | `src/layout/render-svg/shadows.ts:377` | Replaced direct `new Map` return with `intoMap(entries)`. |
| Prelude nullability checks | `src/layout/apply-layout-hints.ts:97` | Replaced repeated explicit undefined checks in hanging-hint, compaction, and median helpers with `fromNullable` + `isNone`/`getOrElse`. |
| Prelude nullability checks | `src/layout/buchheim.ts:171` | Replaced explicit null/undefined checks in contour traversal, sibling resolution, and apportion bookkeeping with Option-based guards while preserving Buchheim behavior. |

#### Checklist

- [x] No nullability checks in any form — `if (x === undefined)`, `if (x !== undefined)`, `if (x === null)`, `if (x !== null)`, `if (!x)`, `x ?? y` — use `fromNullable` / `getOrElse` / `isSome`
- [x] No `x ?? fallback` — use `getOrElse`
- [x] No `try/catch` outside adapter boundaries — use `tryCatch`/`tryCatchAsync`
- [x] No `.map()` on fallible function — use `traverseArray`
- [x] No `new Map()` / `new Set()` — use `intoMap` / `intoSet`
- [x] No `import from 'ramda'`
- [x] Prelude ADTs accessed via exported guards (`isOk`, `isSome`), never `._tag` directly
- [x] No `Result<void, E>` — use `Result<Unit, E>`
- [x] Side effects in pipelines via `tap` / `tapErr`
- [x] Unknown record decoded via `isRecord` + `getStringField`/`getNumberField`/`getTypedField`

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| — | — | — |

### Slice 4 — `src/lexer/**`

**Status:** ✅ Fixed

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| — | — | — | No open findings in this slice. |

#### Resolved

| Rule | Location | What changed |
|------|----------|--------------|
| Prelude nullability checks | `src/lexer/tokenize.ts:52` | Replaced explicit undefined checks in keyword/token/indent resolution with `fromNullable` + `isNone` guards. |

#### Checklist

- [x] No nullability checks in any form — `if (x === undefined)`, `if (x !== undefined)`, `if (x === null)`, `if (x !== null)`, `if (!x)`, `x ?? y` — use `fromNullable` / `getOrElse` / `isSome`
- [x] No `x ?? fallback` — use `getOrElse`
- [x] No `try/catch` outside adapter boundaries — use `tryCatch`/`tryCatchAsync`
- [x] No `.map()` on fallible function — use `traverseArray`
- [x] No `new Map()` / `new Set()` — use `intoMap` / `intoSet`
- [x] No `import from 'ramda'`
- [x] Prelude ADTs accessed via exported guards (`isOk`, `isSome`), never `._tag` directly
- [x] No `Result<void, E>` — use `Result<Unit, E>`
- [x] Side effects in pipelines via `tap` / `tapErr`
- [x] Unknown record decoded via `isRecord` + `getStringField`/`getNumberField`/`getTypedField`

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| — | — | — |

### Slice 5 — `src/parser/**`

**Status:** ✅ Fixed

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| — | — | — | No open findings in this slice. |

#### Resolved

| Rule | Location | What changed |
|------|----------|--------------|
| Prelude nullability checks | `src/parser/grammar.ts:89` | Replaced explicit undefined checks in attribute value parsing with `fromNullable` + `isNone`-based branching. |
| Prelude nullability checks | `src/parser/combinators.ts:178` | Replaced token parser `current !== undefined` guard with `fromNullable` + `isNone`, using narrowed Option value. |

#### Checklist

- [x] No nullability checks in any form — `if (x === undefined)`, `if (x !== undefined)`, `if (x === null)`, `if (x !== null)`, `if (!x)`, `x ?? y` — use `fromNullable` / `getOrElse` / `isSome`
- [x] No `x ?? fallback` — use `getOrElse`
- [x] No `try/catch` outside adapter boundaries — use `tryCatch`/`tryCatchAsync`
- [x] No `.map()` on fallible function — use `traverseArray`
- [x] No `new Map()` / `new Set()` — use `intoMap` / `intoSet`
- [x] No `import from 'ramda'`
- [x] Prelude ADTs accessed via exported guards (`isOk`, `isSome`), never `._tag` directly
- [x] No `Result<void, E>` — use `Result<Unit, E>`
- [x] Side effects in pipelines via `tap` / `tapErr`
- [x] Unknown record decoded via `isRecord` + `getStringField`/`getNumberField`/`getTypedField`

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| — | — | — |

### Slice 6 — `src/resolver/**`

**Status:** ✅ Fixed

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| — | — | — | No open findings in this slice. |

#### Resolved

| Rule | Location | What changed |
|------|----------|--------------|
| Prelude nullability checks | `src/resolver/resolve.ts:176` | Replaced explicit undefined checks across icon/shadow/edge option shaping with `fromNullable` + `isNone` and typed helper combinators. |
| Prelude nullability checks | `src/resolver/validate.ts:71` | Replaced explicit undefined checks for memo/suggestion/type-side validation with Option-based helpers. |
| Prelude nullability checks | `src/resolver/handles.ts:73` | Replaced explicit handle undefined branch with `fromNullable` + `isNone` in handle assignment flow. |

#### Checklist

- [x] No nullability checks in any form — `if (x === undefined)`, `if (x !== undefined)`, `if (x === null)`, `if (x !== null)`, `if (!x)`, `x ?? y` — use `fromNullable` / `getOrElse` / `isSome`
- [x] No `x ?? fallback` — use `getOrElse`
- [x] No `try/catch` outside adapter boundaries — use `tryCatch`/`tryCatchAsync`
- [x] No `.map()` on fallible function — use `traverseArray`
- [x] No `new Map()` / `new Set()` — use `intoMap` / `intoSet`
- [x] No `import from 'ramda'`
- [x] Prelude ADTs accessed via exported guards (`isOk`, `isSome`), never `._tag` directly
- [x] No `Result<void, E>` — use `Result<Unit, E>`
- [x] Side effects in pipelines via `tap` / `tapErr`
- [x] Unknown record decoded via `isRecord` + `getStringField`/`getNumberField`/`getTypedField`

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| — | — | — |

### Slice 7 — `src/style/**`

**Status:** ✅ Fixed

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| — | — | — | No open findings in this slice. |

#### Resolved

| Rule | Location | What changed |
|------|----------|--------------|
| Prelude nullability checks | `src/style/dsl.ts:479` | Replaced explicit null/undefined checks in selector/variable/rule application paths with `fromNullable` + `isNone`. |
| Prelude `??` fallback | `src/style/packs.test.ts:18` | Replaced nullish fallback with `getOrElse` + `fromNullable`. |
| Prelude nullability checks | `src/style/packs.test.ts:22` | Replaced undefined assertions with `fromNullable` + `isNone`. |

#### Checklist

- [x] No nullability checks in any form — `if (x === undefined)`, `if (x !== undefined)`, `if (x === null)`, `if (x !== null)`, `if (!x)`, `x ?? y` — use `fromNullable` / `getOrElse` / `isSome`
- [x] No `x ?? fallback` — use `getOrElse`
- [x] No `try/catch` outside adapter boundaries — use `tryCatch`/`tryCatchAsync`
- [x] No `.map()` on fallible function — use `traverseArray`
- [x] No `new Map()` / `new Set()` — use `intoMap` / `intoSet`
- [x] No `import from 'ramda'`
- [x] Prelude ADTs accessed via exported guards (`isOk`, `isSome`), never `._tag` directly
- [x] No `Result<void, E>` — use `Result<Unit, E>`
- [x] Side effects in pipelines via `tap` / `tapErr`
- [x] Unknown record decoded via `isRecord` + `getStringField`/`getNumberField`/`getTypedField`

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| — | — | — |

### Slice 8 — `src/reporting-chain.*`, `src/span-of-control.*`, `src/subordinate-count-policy.*`, `src/subtree.*`

**Status:** ✅ Fixed

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| — | — | — | No open findings in this slice. |

#### Resolved

| Rule | Location | What changed |
|------|----------|--------------|
| Prelude nullability checks | `src/span-of-control.ts:202` | Replaced `fte !== undefined` branch with `fromNullable` + `isNone` manual override flow. |

#### Checklist

- [x] No nullability checks in any form — `if (x === undefined)`, `if (x !== undefined)`, `if (x === null)`, `if (x !== null)`, `if (!x)`, `x ?? y` — use `fromNullable` / `getOrElse` / `isSome`
- [x] No `x ?? fallback` — use `getOrElse`
- [x] No `try/catch` outside adapter boundaries — use `tryCatch`/`tryCatchAsync`
- [x] No `.map()` on fallible function — use `traverseArray`
- [x] No `new Map()` / `new Set()` — use `intoMap` / `intoSet`
- [x] No `import from 'ramda'`
- [x] Prelude ADTs accessed via exported guards (`isOk`, `isSome`), never `._tag` directly
- [x] No `Result<void, E>` — use `Result<Unit, E>`
- [x] Side effects in pipelines via `tap` / `tapErr`
- [x] Unknown record decoded via `isRecord` + `getStringField`/`getNumberField`/`getTypedField`

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| — | — | — |

### Slice 9 — `src/types/**`

**Status:** ✅ Clean

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| — | — | CLEAN | No prelude anti-patterns matched in `src/types/**`. |

#### Checklist

- [x] No nullability checks in any form — `if (x === undefined)`, `if (x !== undefined)`, `if (x === null)`, `if (x !== null)`, `if (!x)`, `x ?? y` — use `fromNullable` / `getOrElse` / `isSome`
- [x] No `x ?? fallback` — use `getOrElse`
- [x] No `try/catch` outside adapter boundaries — use `tryCatch`/`tryCatchAsync`
- [x] No `.map()` on fallible function — use `traverseArray`
- [x] No `new Map()` / `new Set()` — use `intoMap` / `intoSet`
- [x] No `import from 'ramda'`
- [x] Prelude ADTs accessed via exported guards (`isOk`, `isSome`), never `._tag` directly
- [x] No `Result<void, E>` — use `Result<Unit, E>`
- [x] Side effects in pipelines via `tap` / `tapErr`
- [x] Unknown record decoded via `isRecord` + `getStringField`/`getNumberField`/`getTypedField`

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| — | — | — |

### Slice 10 — `src/tests/factories/**`

**Status:** ✅ Fixed

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| — | — | — | No open findings in this slice. |

#### Resolved

| Rule | Location | What changed |
|------|----------|--------------|
| Prelude nullability checks | `src/tests/factories/layout-slice.ts:44` | Replaced optional layout hint shaping with typed Option helper (`fromNullable` + `isNone`). |
| Prelude `??` fallback | `src/tests/factories/layout-slice.ts:108` | Replaced nullish fallback for children/staff with `getOrElse` + `fromNullable`. |
| Prelude `??` fallback | `src/tests/factories/parser-slice.ts:16` | Replaced token value `??` fallback with `getOrElse` + `fromNullable`. |

#### Checklist

- [x] No nullability checks in any form — `if (x === undefined)`, `if (x !== undefined)`, `if (x === null)`, `if (x !== null)`, `if (!x)`, `x ?? y` — use `fromNullable` / `getOrElse` / `isSome`
- [x] No `x ?? fallback` — use `getOrElse`
- [x] No `try/catch` outside adapter boundaries — use `tryCatch`/`tryCatchAsync`
- [x] No `.map()` on fallible function — use `traverseArray`
- [x] No `new Map()` / `new Set()` — use `intoMap` / `intoSet`
- [x] No `import from 'ramda'`
- [x] Prelude ADTs accessed via exported guards (`isOk`, `isSome`), never `._tag` directly
- [x] No `Result<void, E>` — use `Result<Unit, E>`
- [x] Side effects in pipelines via `tap` / `tapErr`
- [x] Unknown record decoded via `isRecord` + `getStringField`/`getNumberField`/`getTypedField`

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| — | — | — |

---

## Top 3 Priority Issues

1. None — all audited prelude MUST issues are resolved.
2. None.
3. None.
